#include <stdio.h>

int main(){
	int a,b,c,media,soma;
	printf("Digite as notas bimestrais!\n");
	scanf("%d %d %d", &a, &b, &c;
	soma = (a+b+c);
	media = (soma/3);
	printf("%d\n", media);
	return 0;
}

