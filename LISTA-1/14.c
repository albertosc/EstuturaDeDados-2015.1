#include <stdio.h>

int main(){
	int salatual, novosal;
	printf("Digite o salario atual!");
	scanf("%d", &salatual);
	novosal = (salatua + (salatual*0,25);
	printf("O novo salrio é %d", novosal);
	return 0;
}
